// Container with most water

#include<stdio.h>

int maxArea(int arr[],int size_arr)
{
    
}








int main()
{
    int arr[]= {1,8,6,2,5,4,8,3,7};
    int size_arr = sizeof(arr)/sizeof(arr[0]);
    maxArea(arr, size_arr);
    return 0;

}