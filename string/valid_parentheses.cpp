// C++ program to check for balanced brackets.
 
#include <bits/stdc++.h>
using namespace std;

int areBracketsBalanced(string expr)
{
    stack<char> temp;
    n = exp.length();
    for(int i = 0; i < n; i++)
        if(temp.empty(){
            //IF stack is empty then push brackets into it
            temp.push(exp[i])
        })
        else if 

}




// Driver code
int main()
{
    string expr = "{()}[]";
 
    // Function call
    if (areBracketsBalanced(expr))
        cout << "Balanced";
    else
        cout << "Not Balanced";
    return 0;
}